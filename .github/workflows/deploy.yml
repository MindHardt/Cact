name: Deploy
on:
  push:
    branches:
      - master
jobs:

  build:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_TOKEN }}

      - name: Build and push pocketbase
        uses: docker/build-push-action@v6
        with:
          push: true
          context: .
          file: ./pocketbase/Dockerfile
          tags: ${{ secrets.REGISTRY }}/cact/pocketbase:latest

      - name: Build and push web
        uses: docker/build-push-action@v6
        with:
          push: true
          context: .
          file: ./web/Dockerfile
          build-args: |
            NODE_ENV=production
            VITE_POCKETBASE_URL=https://cactpb.un1ver5e.ru
          tags: ${{ secrets.REGISTRY }}/cact/web:latest

      - name: Calling portainer webhooks
        run: |
          sleep 10s
          curl -fail -X POST ${{ secrets.POCKETBASE_WEBHOOK_URL }}
          curl -fail -X POST ${{ secrets.WEB_WEBHOOK_URL }}